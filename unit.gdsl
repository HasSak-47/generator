model Foo {
    name: string,

    down: bool,
    available: bool,
}

model Bar {
    name: string,
    servers: Foo[],
}

model Baz{
    start: string as datetime,
    end: string as datetime,
}

get_bar_state(name: string) @get "/api/v1/bar/{name}/state" -> Bar
get_bar_id(name: string) @get "/api/v1/bar/{name}/id" -> uint
get_bar_from_baz(baz: Baz) @get "/api/v1/bar/from/baz" -> Bar
