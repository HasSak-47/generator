model Foo {
    name: string,

    down: bool,
    available: bool,
}

model Bar {
    name: string,
    servers: Foo[],
}

model Baz{
    start: string as datetime,
    end: string as datetime,
}

send_message(name: string) @get "/message/"
get_bar_state(name: string) @get "/bar/{name}/state" -> Bar
get_bar_id(name: string) @get "/bar/{name}/id" -> uint
get_bar_from_baz(baz: Baz) @get "/bar/from/baz" -> string
